public interface PlayerBoard {

    //public void makeMove();
    /* is the interface necessary? we would use only ChessGame to collect all methods and rules from a single turn
    to set an input
    if you want to add the captured piece to the pieceEaten array, it's also possible
    It's possible, if we implement a simple interface, there would be maybe 1 point more
     */
}
